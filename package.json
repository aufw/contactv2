{
  "name": "simple-contacts",
  "version": "2.0.0",
  "description": "Interview Contacts v2",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "serve": "npm run build && copy api.php dist\\ && cd dist && php -S localhost:8000"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.8",
    "typescript": "^5.6.2"
  },
  "type": "module"
}